# TEST FILE - Check if .htaccess is being read
# If you see "500 Internal Server Error" when visiting your site,
# it means .htaccess IS being read (syntax error is intentional)

INTENTIONAL_ERROR_TO_TEST_IF_HTACCESS_WORKS

# If you still see your site normally, .htaccess is NOT being read
# This means AllowOverride is set to None
